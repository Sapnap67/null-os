# NULL.OS 用户手册

## 项目介绍

`NULL.OS` 是一套完全在浏览器中运行的科幻悬疑解谜合集。当前包含三个独立案件：失踪研究员、零号站末班车和记忆档案馆。每案都有不同故事、线索、终端命令、密码谜题与结局。

## 启动方法

双击项目根目录中的 `index.html`，使用 Chrome、Edge 或其他现代浏览器打开。不需要安装、不需要账号，也不需要联网。

## 游戏方法

- 开机后会进入普通电脑风格的首页桌面，点击“案件中心 / CASE HUB”。
- 在案件中心点击“接受任务”；有进度的案件会显示“继续调查”。
- “已完成”只显示已经结案的任务；“档案库”汇总每案的证据和结案状态。
- 双击或单击桌面图标打开应用。
- 拖动窗口标题栏可以移动窗口。
- 阅读文件和邮件，点击“标记为证据”。
- 在终端输入 `help` 查看可用命令。
- 根据不同应用中的线索推导保险库密码。密码固定使用“英文代号-4位数字”，例如 `CODE-1234`；大小写不限，漏写短横线会自动补全。
- 找齐五条证据后选择结局。
- 点击调查桌面左上角 `N//` 可随时回到首页桌面；三个案件分别保存进度。
- 全站固定使用黑色背景与蓝色图标的深色主题。

## 存档与重置

游戏使用浏览器本地存储分别保存每个案件的证据、保险库状态和结局。点击右下角 `RESET CASE` 只重置当前案件，不影响另外两个案件。

## 课堂展示建议

先展示开机动画和伪桌面，再打开邮件与终端说明网页的互动功能，最后输入保险库密码并展示结局选择。完整游玩约 10 至 20 分钟。

## 文件说明

- `index.html`：页面结构和应用窗口模板。
- `styles.css`：界面、动画、布局与颜色。
- `app.js`：窗口交互、故事、谜题、存档和结局逻辑。

## 故障排查

- 页面没有更新：按 `Ctrl+F5` 强制刷新。
- 想重新开始：点击 `RESET CASE`。
- 存档没有保留：确认浏览器没有禁止本地存储，且不是隐私浏览模式。
